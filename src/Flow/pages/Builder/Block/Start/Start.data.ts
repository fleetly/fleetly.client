// Components
import { BlockStartKeyword } from './components/Keyword';

// Interfaces
import { ElementType } from '@flow/interfaces';

export const START_ELEMENTS = [
  {
    Component: BlockStartKeyword,
    title: 'Message keyword',
    type: ElementType.START_KEYWORD
  }
];
