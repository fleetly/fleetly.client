query getChannelList($companyId: String!) {
  channels(companyId: $companyId) {
    id
    source {
      id
      link
      name
      photo
      title
      type
    }
    status {
      color
      message
      type
    }
  }
}
