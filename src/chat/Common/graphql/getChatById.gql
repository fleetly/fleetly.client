query getChatById($chatId: String!) {
  chat(chatId: $chatId) {
    id
    lastMessage {
      id
      author {
        id
        firstname
      }
      date
      isComment
      text
    }
    status
    subscriber {
      id
      source {
        firstname
        lastname
        photo
        type
      }
    }
  }
}
